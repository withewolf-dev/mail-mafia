# env / secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# node
node_modules/
dist/
build/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local data — never commit harvested prospects or reply dumps
data/
*.local.json
*.csv

# crawled pages — working folder, replaced by a Neon table later
crawl-sites/

# AI Overview screenshots. Working evidence, not source: they are large PNGs
# that change on every re-capture, and ai_overviews.screenshot_path in Neon is
# the record of what was read. Re-run src/cli/ai-overview.ts to regenerate.
ai-overviews/

# os / editor
.DS_Store
.vscode/
.idea/

# installed agent skills — reinstall with `npx skills add`, the lockfile pins them
.agents/

# per-machine Claude Code state: local permission grants and session checkpoints
.claude/settings.local.json
.claude/RESUME.md
